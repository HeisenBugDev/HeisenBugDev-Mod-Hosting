/ header
/   if userSignedIn
/     link-to "user" signedInUser | Welcome {{signedInUser.name}}
/     div click='signOut'
/       link-to "sign-out" |  Sign out
/   else
/     link-to "sign-in" | Sign in
/ header
/ a click="signOut" href="#" Sign Out
render 'auth'

nav
  link-to 'users.index'
    | Users list

main
  outlet